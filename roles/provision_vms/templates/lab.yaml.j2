kvm:
  vms:
{% for host in groups['vms'] %}
    - vm_name: {{ hostvars[host]['vm_name'] }}
      os: linux
      master_image: {{ lab_path }}/{{ hostvars[host]['vm_name'] }}.qcow2
      force_copy: false
      memory: 2048
      tapnum: {{ hostvars[host]['tapnum'] }}
{% endfor %}
